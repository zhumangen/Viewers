/**
 * @deprecated Floating secondary pill removed from Zelvyn shell (not in mockup).
 * Overflow tools live under ZelvynToolRail "More" (toolbar section `moreTools`).
 * Kept as a no-op export so older imports do not break.
 */
export function ZelvynToolPill() {
  return null;
}

export default ZelvynToolPill;
