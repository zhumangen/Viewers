Template.toolbarSectionTools.events({
  /*'click .js-view-more label'(event) {
        const $target = $(event.currentTarget);
        const toToggle = $target.data('tool');
        const instance = Template.instance();

        if (toToggle == 'hanging') {console.log(Template.viewer.data.state);
          instance.data.state.set('leftSidebar', $target.find('input').prop('checked') ? 'studies' : null);
        }
    }
    'mouseenter .toolbarSectionButton'(event, instance) {
        //$(event.currentTarget).find('.buttonLabel').addClass('activeButton');
    },
    'mouseleave .toolbarSectionButton'(event, instance) {
        //$(event.currentTarget).find('.buttonLabel').removeClass('activeButton');
    }*/
});
